"use strict"import React from 'react';import {connect} from 'react-redux';import {bindActionCreators} from 'redux';import {Image, Row, Col, Well, Button} from 'react-bootstrap';import {playRock, playPaper, playScissors} from '../../actions/rpsActions';class SelectRps extends React.Component {    handleSelect(selected) {        if (selected == 'rock') this.props.playRock('player1');        if (selected == 'scissors') this.props.playScissors('player1');        if (selected == 'paper') this.props.playPaper('player1');        setTimeout(() => {            var rand = Math.floor(Math.random() * ( 3 ));            if (rand == 0) this.props.playRock('player2')            else if (rand == 1) this.props.playScissors('player2');            else this.props.playPaper('player2');        },1000)    }    render() {        return (            <Well>                <Row>Please select one element</Row>                <Row>                    <Col xs={4}>                        <Button bsStyle='primary' onClick={this.handleSelect.bind(this, 'rock')}>ROCK</Button>                    </Col>                    <Col xs={4}>                        <Button bsStyle='primary' onClick={this.handleSelect.bind(this, 'scissors')}>SCISSORS</Button>                    </Col>                    <Col xs={4}>                        <Button bsStyle='primary' onClick={this.handleSelect.bind(this, 'paper')}>PAPER</Button>                    </Col>                </Row>            </Well>        )    }}function mapDispatchToProps(dispatch) {    return bindActionCreators({playRock, playPaper, playScissors}, dispatch)}export default connect(null, mapDispatchToProps)(SelectRps)